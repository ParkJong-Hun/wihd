# Compiler metrics
Dir.glob('**/compose_compiler_metrics').reject { |path| path.start_with?('app-component-catalog/') }.each do |report_dir|
  next if report_dir.include?("vendor/bundle")
  compose_compiler_metrics.report(report_dir)
end